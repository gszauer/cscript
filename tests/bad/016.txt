// This should fail because a global variable must be constant

int iVal = 2 + 4 * (9 - 3);
int iDefault = iVal;

float fVal = 3.14;
float fDefault;

char cVal = 't';
char cDefault;

bool bVal = true;
bool bDefault;

void run() {
	// These should fail
}