// Trying to set a variable to void as an assignment

int iVal = 2 + 4 * (9 - 3);
int iDefault;

float fVal = 3.14;
float fDefault;

char cVal = 't';
char cDefault;

bool bVal = true;
bool bDefault;

void run() {
	float fFloat = 0.0;
	fFloat = void;
}